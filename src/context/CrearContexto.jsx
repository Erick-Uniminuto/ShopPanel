import { createContext } from "react";
export const CrearContexto = createContext()

// Creo el contexto que se encargara de obtener las credenciales del usuario
// que esta intentando iniciar sesion.